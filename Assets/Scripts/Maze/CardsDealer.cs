using UnityEngine;

public class CardsDealer : MonoBehaviour
{
    // Gives player a random set of 3 mods after clearing a node
    
    // Or a corruption when player moves a certain amount of tiles in the maze
    
    // Cards have 3 properties:
    // 1. bit cost
    // 2. effect
    
    /// <summary>
    /// Will give the player a set of 3 buffs to choose from
    /// </summary>
    public void GiveBuff()
    {
        
    }

    /// <summary>
    /// Corruptions are risky buffs after moving a certain amount of tiles in the maze
    /// </summary>
    public void GiveCorruption()
    {
        
    }
}
